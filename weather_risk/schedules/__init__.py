# Define Dagster schedules here and add them to the `schedules` list.
# Example:
#   from dagster import ScheduleDefinition
#   from weather_risk.jobs import weather_pipeline_job
#
#   daily_weather_schedule = ScheduleDefinition(
#       job=weather_pipeline_job,
#       cron_schedule="0 6 * * *",
#   )

schedules = []
